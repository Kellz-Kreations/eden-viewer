# Copy to .env and update with your values
# Find PUID/PGID with: id $(whoami)

# Synology host paths
APPDATA_ROOT=/volume1/docker/appdata
DATA_ROOT=/volume1/data
TRANSCODE_ROOT=/volume1/docker/transcode

# Legacy variables (only needed for docker-compose.yml compatibility)
APPDATA_PATH=/volume1/docker/appdata
DATA_PATH=/volume1/data

# DSM user/group that owns the above folders
PUID=1026
PGID=100

# Timezone (example)
TZ=America/New_York

# Plex claim token - REQUIRED for first-time remote/headless setup!
# 1. Visit https://www.plex.tv/claim/ (must be logged in)
# 2. Copy the token (looks like: claim-xxxxxxxxxxxxxxxxxxxx)
# 3. Paste below and deploy within 4 minutes (token expires quickly)
# After initial claim, you can remove or leave blank
PLEX_CLAIM=

# Optional: where Plex advertises itself (usually not needed on host network)
ADVERTISE_IP=
